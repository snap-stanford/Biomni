# QA Datasets

이 디렉토리에는 HITS AI Agent를 평가하기 위한 QA 태스크들이 저장됩니다.

## 📁 태스크 구조

각 태스크는 다음과 같은 구조를 가집니다:

```
task_XXX/
├── question.md       # 질문 (필수)
├── answer.md         # 정답 (필수)
├── metadata.json     # 메타데이터 (선택)
├── input_data.csv    # Input data 파일 (선택, 분석에 필요한 경우)
├── image1.png        # 이미지 파일 (선택, 있는 경우 task 폴더 바로 아래)
└── image2.png
```

**주의**: 
- 모든 파일(이미지, input data 등)은 별도 폴더가 아닌 **task 폴더 바로 아래**에 위치시킵니다.
- Input data 파일은 CSV, TXT, FASTA 등 다양한 형식 가능합니다.

## 📝 필수 파일

### 1. question.md
질문을 마크다운 형식으로 작성합니다.

```markdown
# 질문 제목

질문 내용을 작성합니다...
```

### 2. answer.md
정답을 마크다운 형식으로 작성합니다.

```markdown
# 정답 제목

## 섹션 1
내용...

![이미지 설명](image1.png)
```

**이미지 및 데이터 참조**: 파일은 같은 폴더에 있으므로 파일명만 사용
- ✅ 올바름: `![image](image.png)` 또는 `data.csv`
- ❌ 잘못됨: `![image](images/image.png)` 또는 `data/data.csv`

### 3. metadata.json (선택)
태스크의 메타정보를 JSON 형식으로 작성합니다.

```json
{
  "category": "genetics",
  "difficulty": "medium",
  "created_at": "2024-11-24T12:00:00",
  "description": "Brief description of the task",
  "tags": ["tag1", "tag2"],
  "expected_tools": ["tool1", "tool2"],
  "requires_images": true,
  "expected_images": ["image1.png", "image2.png"],
  "requires_input_data": true,
  "input_data_files": ["data.csv", "sequences.fasta"],
  "input_data_description": "Description of input data files"
}
```

### 4. Input Data 파일 (선택)
분석에 필요한 입력 데이터 파일입니다. 다양한 형식 지원:
- **CSV/TSV**: 표 형식 데이터
- **FASTA/FASTQ**: 시퀀스 데이터
- **VCF/BED**: 유전체 데이터
- **JSON/XML**: 구조화된 데이터
- 기타: TXT, XLSX 등

**Input data는 question.md에서 명시적으로 언급되어야 합니다.**

## 🆕 새 태스크 추가하기

```bash
# 1. 새 태스크 폴더 생성
mkdir qa_datasets/task_003

# 2. 질문 작성
cat > qa_datasets/task_003/question.md << 'EOF'
# 여기에 질문 작성
EOF

# 3. 정답 작성
cat > qa_datasets/task_003/answer.md << 'EOF'
# 여기에 정답 작성
EOF

# 4. Input data 추가 (있는 경우)
cp /path/to/data.csv qa_datasets/task_003/

# 5. 이미지 추가 (있는 경우)
cp /path/to/image.png qa_datasets/task_003/

# 6. 메타데이터 작성 (선택)
cat > qa_datasets/task_003/metadata.json << 'EOF'
{
  "category": "your_category",
  "difficulty": "medium",
  "requires_input_data": true,
  "input_data_files": ["data.csv"]
}
EOF
```

## 📊 현재 태스크

### task_001: 유전자 정보 검색
- **카테고리**: genetics
- **난이도**: easy
- **설명**: BRCA1 유전자에 대한 기본 정보 검색
- **Input data**: 없음
- **이미지**: 없음

### task_002: 통계 분석
- **카테고리**: statistics
- **난이도**: medium
- **설명**: 기본 통계 분석 및 시각화
- **Input data**: 없음
- **이미지**: histogram.png, boxplot.png

### task_003: 유전자 발현 데이터 분석
- **카테고리**: bioinformatics
- **난이도**: medium
- **설명**: CSV 데이터를 분석하여 DEG 찾기 및 시각화
- **Input data**: gene_expression_data.csv
- **이미지**: fold_change_plot.png

## 🔍 태스크 네이밍 규칙

- `task_001`, `task_002`, ... 형식 권장
- 3자리 숫자로 고유 ID 부여
- 설명적인 이름도 가능: `rna_seq_analysis`, `gwas_study` 등

## ✅ 베스트 프랙티스

1. **명확한 질문**: 무엇을 원하는지 구체적으로 작성
2. **상세한 정답**: AI가 생성해야 할 내용의 예시를 충분히 제공
3. **Input data 명시**: 질문에서 어떤 데이터 파일을 사용해야 하는지 명확히 기재
4. **이미지 참조**: 마크다운에서 정확한 파일명 사용
5. **메타데이터**: 카테고리, 난이도, input data 정보를 명시하여 관리 용이하게
6. **테스트**: 새 태스크 추가 후 반드시 실행하여 검증

## 🚫 주의사항

- 모든 파일은 별도 폴더가 아닌 task 폴더 직접 아래에 위치
- 마크다운에서 파일 참조시 경로 포함하지 말 것 (파일명만 사용)
- Input data 파일명이 `question.md`, `answer.md`, `metadata.json`과 겹치지 않도록
- 파일명에 공백이나 특수문자 사용 지양
- UTF-8 인코딩 사용
- 대용량 파일(>100MB)은 피하고, 필요시 샘플 데이터 사용

---

더 자세한 내용은 [README.md](../README.md)를 참고하세요.

