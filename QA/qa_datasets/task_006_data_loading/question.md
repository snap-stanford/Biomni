Please load the data from `input_data/sample_data.tsv`.

I'd like to see some basic summary statistics (mean, std, min, max) for the numerical columns and know the dimensions of the dataset. Also, show me the first few rows so I can see what the data looks like.
