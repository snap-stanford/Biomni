apiVersion: v1
kind: Service
metadata:
  name: {{ include "coral.fullname" . }}
  labels:
    {{- include "coral.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coral.selectorLabels" . | nindent 4 }}
