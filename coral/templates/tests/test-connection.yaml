apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coral.fullname" . }}-test-connection"
  labels:
    {{- include "coral.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coral.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
