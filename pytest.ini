[pytest]
# Integration tests hit live external APIs (ClinicalTrials.gov, etc.).
# They are opt-in: run with `pytest -m integration` to include them.
markers =
    integration: tests that call live external APIs (opt-in)
addopts = -m "not integration"
